# Project Context 1.7.4 发布验收

日期：2026-09-11。发布工作区为独立 worktree，不包含原仓库已有的 .agent 暂存与删除。

## 修改

- 架构初始化和交接读取规范 Markdown 中的确认依据，不要求 baselineStatus、批准指纹或 C4 模型。
- 区分当前基线、目标设计、实施与验证；Project Context 只观察，不创建批准。
- 新增 Markdown-only 架构发现回归，确认初始化不会改写基线。
- 仅版本常量改变运行时代码，其余为技能说明、README、变更记录和测试。既有历史 C4 夹具保留，不作为新工作流证明。

## 执行证据

Luna 子代理 context_acceptance_luna 执行：
- npm test：退出码 0，TypeScript 构建通过，原完整套件 94/94 通过。
- npm run package:local：退出码 0，本地 marketplace 生成，manifest/package 为 1.7.4。
- git diff --check：通过。

初次验收指出初始化说明仍引用固定字段，以及本发布记录缺失。已修订说明并补充本记录。

Luna 修复后复核：
- node --test tests/project-context.test.mjs：退出码 0，13/13 通过，含新增 Markdown-only 发现与基线原文保留用例。
- node scripts/prepare-local-marketplace.mjs：退出码 0，复用已有 dist 刷新安装包；版本一致，docs 按预期不进入安装包。
- git diff --check：通过。最终结论：发布范围验收通过。

94/94 是补充用例之前的完整套件结果；13/13 是修正后受影响测试文件结果，不将两次执行描述为一次全量运行。

## 范围限制

自动测试覆盖运行时发现、初始化、状态保存与既有行为；不证明模型在真实 Codex 会话中正确理解所有设计。
未安装到全局 Codex，未执行真实硬件验证。历史夹具不是固件证据。
GitHub Release 同时提供 marketplace ZIP 和 SHA256；发布包不包含整个源码 docs 目录，本记录随仓库与发布说明提供。
