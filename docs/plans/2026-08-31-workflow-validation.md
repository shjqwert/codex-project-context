# P004 源码阶段验收

日期：2026-08-31。源码、文档及 Codex 安装已完成，本地提交已生成；两个仓库的推送受本地分支命名钩子阻塞。RAG 不在本轮范围。

## 检索评测

候选、aliases、查询和标签在修改算法前冻结：21 个工作、208 条查询，开发集与保留集各104条，负例合计48条。SHA-256：`1b31ce2b016823a7c682cbdbc60cb63396e764b47bc20f4738e4fa19e71a95d3`。

| 保留集指标 | 旧版本基线 | 新实现 |
|---|---:|---:|
| 自动读取精确率 | 66.14% | 100% |
| 允许自动读取工作项召回率 | 100% | 97.62% |
| 单目标 Top-1 | 94.12% | 97.06% |
| 多目标完整率 | 100% | 100% |
| 无匹配负例误召回率 | 50% | 0% |
| 歧义保留率 | 0% | 100% |

开发集最终各正向指标100%、负例误召回0。保留集仍有2条正例漏召回；通过已冻结门槛，不因看到这些漏召回再改算法或标签。此为本地策划的固定语料验收，不是独立盲测或通用语义准确率，也不是实际token账单。

可复核产物：

- `tests/fixtures/retrieval/corpus.json` 与 `manifest.json`：固定语料和哈希。
- `scripts/evaluate-retrieval.mjs`：指标实现和门槛。
- `retrieval-baseline-development.json`、`retrieval-baseline-holdout.json`：旧构建基线。
- `retrieval-final-development.json`、`retrieval-final-holdout.json`：最终结果及未命中项。

已修复主题词表外信息丢失、重叠中文片段夸大证据、模块名压过具体目标、明确排除、带主题续接和局部歧义。候选转为可靠时仍能注入，同一结果不会重复读取。新增 `match --explain` 只读诊断不修复索引或checkpoint。

## 实际验证

- 新源码执行 `npm run build` 后运行完整77项 Node测试：77/77通过，包含真实临时目录的索引不写入、Hook候选升为可靠、重复ID评价器负例。
- 1000条工作项的Hook路径本次约526ms，低于现有5秒门槛；这是本机一次测试观察，不是跨机器延迟保证。
- 受影响7个Skill的 `quick_validate.py` 通过；29个内部Markdown引用存在。
- 架构技能主文件从409行缩到87行；状态、建模和交接细节转入3份阶段参考。没有按行数推断token节省。
- C4离线示例 `likec4 validate --json --no-layout tests/fixtures/c4-observations` 返回valid=true、零错误。
- 独立Sol/xhigh验证按15个离线场景作出与验收预期一致的判定，覆盖模型/实现/build/report变化、缺少证据、非法状态、正常关闭和未初始化。只证明规则应用，未运行真机或自动后台状态同步。

## 独立实现检查

检索独立验证初次发现5类缺陷：礼貌前缀/混合排除、其它目标解除局部歧义、CAN等短主题被当作空续接、诊断路径重建索引、重复ID使指标假通过。主会话复现并按已确认验收要求修复，增加回归测试。

同一验证者对这5类原缺陷限定复核，结论PASS；未将此结论外推成完整语义准确率保证。独立C4验证未发现此次拆分丢失批准、证据、归档或设备安全门槛。

## Sol 独立维护

补入历史提交293266924b的精确LF/CRLF模板哈希，继续拒绝未知定制；新增只读安装检查，分别验证Codex注册、插件缓存和7个代理配置。静态验证通过，3项安装测试通过，含已知版本升级、未知修改拒绝、失败回滚和选定CODEX_HOME的隔离。

安装前检查正确报告新增脚本尚未进入旧缓存；本轮安装后复查43个插件文件和7个代理均与来源一致，Codex注册为启用。检查器只报告，不自动覆盖。编排核心未扩展，Exa/MarkItDown移除状态保留。

## 已授权的打包与安装交付

- Project Context安装版本为`1.5.0+codex.20260831084613`，源码、安装包与缓存的101个文件逐字节一致。
- Sol安装版本为`0.12.2+codex.20260831083501`；4个嵌入式工作流技能已同步，旧文件备份在用户Codex backups目录。
- 新app-server实例的skills/list实际发现新版插件和技能；两个Project Context Hook均enabled且trusted，未绕过信任检查。
- 发布检查发现遗留空Skill目录导致无效打包，已修复：保留源目录，跳过空目录树，非空无SKILL.md目录在替换产物前拒绝。
- 并行全套测试曾出现一次Windows锁创建EPERM；最小并发测试单独通过。增加Windows创建EPERM有界重试，不据此删除锁，持续拒绝仍失败且不执行受保护操作。相应故障注入测试通过，最终全量80/80通过。
- 冻结语料增加LF检出属性；启用core.autocrlf的隔离检出后仍符合原冻结SHA-256。未改变查询、标签、候选或阈值。

## Git交付状态

- Skills与Sol已分别提交；Project Context检索、状态指引及检出属性也已分开提交。
- Sol提交`255729d`已推送main；embedded_skills的`9303e15`与Project Context的本地提交等待推送。
- embedded_skills和codex-project-context具有相同的本地pre-push钩子，只允许`branchs/R工号_缩写/...`，拒绝main。本轮未绕过或修改钩子，未强推；待用户决定处理方式，P004保持in-progress。
- 工作区索引和临时图片未加入提交；未进行硬件操作、npm发布或GitHub Release发布。
