interface:
  display_name: "Project Sync"
  short_description: "Explicitly refresh managed project context."
  default_prompt: "Use $project-sync to inspect this initialized repository and synchronize its evidence-backed Agent-authored context."

policy:
  allow_implicit_invocation: false
