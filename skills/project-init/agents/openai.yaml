interface:
  display_name: "Project Init"
  short_description: "Explicitly initialize durable project context."
  default_prompt: "Use $project-init to inspect this confirmed repository, analyze it with available project tools, and initialize evidence-backed durable context."

policy:
  allow_implicit_invocation: false
