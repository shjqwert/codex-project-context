interface:
  display_name: "Project Init"
  short_description: "Explicitly initialize durable project context."
  default_prompt: "Use $project-init to initialize this confirmed project for durable Codex context."

policy:
  allow_implicit_invocation: false
