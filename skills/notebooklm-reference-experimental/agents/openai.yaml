interface:
  display_name: "NotebookLM Reference Experimental"
  short_description: "Explicitly test isolated NotebookLM reference workflows."
  default_prompt: "Use $notebooklm-reference-experimental to explicitly test isolated NotebookLM search, refresh, upload, or maintenance."
dependencies:
  tools:
    - type: "mcp"
      value: "notebooklm"
      description: "Search and maintain NotebookLM notebooks, sources, and notes."
policy:
  allow_implicit_invocation: false
